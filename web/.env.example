# Base URL of the Layer-2 FastAPI service. NEXT_PUBLIC_ is required for a client component
# (build-time inlined) — it is a non-secret URL. Local dev: http://localhost:8000; prod origin is
# injected by the GitHub Action build env (M4.3-01).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
